services:
  db:
    image: postgres:15.3
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
    - "5432:5432"
    
  frontend:
    build:
      context: ./paggo-frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./paggo-frontend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    depends_on:
      - backend
    restart: unless-stopped
    
  backend:
    build:
      context: ./paggo-backend
      dockerfile: Dockerfile
    ports:
      - "3001:3000"
    volumes:
      - ./paggo-backend:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - DATABASE_URL=${DATABASE_URL}
    depends_on:
      - db
    restart: unless-stopped